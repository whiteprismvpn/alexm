# Deployment

This change requests a fresh deployment of the current `main` branch.

The current resume UI removes the former top header panel and the circular AM badge. The persistent **Get in touch** button remains available while scrolling.
